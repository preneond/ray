[project]
name = "worker-global-state-demo"
version = "0.1.0"
description = "Demonstrates Ray worker process reuse and global state persistence within a job"
requires-python = ">=3.10"
dependencies = [
    "ray>=2.0",
]

[dependency-groups]
dev = []
